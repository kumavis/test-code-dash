# Dailies — 2026-06-12

## Carried forward

Nothing — first session in this repository. The repo contains only
`GENERIC_CLAUDE_WORKFLOW.md`; this session binds that generic methodology to
the Code Analysis Dashboard track.

## Session log

### Track setup

- Created the track design doc
  (`2026-06-12_CODE_ANALYSIS_DASHBOARD_DESIGN.md`) with progress tracker,
  architecture, output-model contract, phase dependency rationale, and test
  strategy. The user supplied a Stage-3-level design summary (10 layers +
  UX + tool suggestions), so Stages 1–2 were satisfied by the input; scope
  decisions and deferrals are recorded in the design doc and `DEFERRED.md`.
- Deferred in the same commit: CFG, DFG, runtime profiling, coverage
  ingestion (genuine dependency/feasibility blockers, not convenience
  deferrals — each has an unblock condition).
- Decision: vanilla-JS renderer inlined into a single generated HTML file,
  `typescript` as the only runtime dependency. Rationale in design doc.

### Phase 0 (commit 4cc77d6)

- Scaffold + 8-file fixture + acceptance spec with commented aspirational
  sections. Surprise: `node --test test/` failed on Node 22 treating the
  directory as a CJS module; fixed with an explicit `test/*.test.mjs` glob.

### Phase 1

- `loadProject` trusts a tsconfig only if it sits INSIDE the analyzed root —
  `ts.findConfigFile` searches upward and would otherwise hijack analysis of
  tsconfig-less projects with an ancestor config (found by reasoning, worth
  a test later if it regresses).
- Module edges from static imports/re-exports + `require()`/dynamic
  `import()` literals, resolved via `ts.resolveModuleName` so path mapping
  works for free. Cycles via Tarjan SCC (>1-member components only).

### Phase 2 (after 88407ed)

- Symbol table keeps the declaration `node` internally (SymbolTable) and is
  stripped before serialization — downstream layers (calls, complexity)
  resolve against nodes, the model stays JSON-clean.
- Gotcha: JSDoc on `const` attaches to the VariableStatement, not the
  VariableDeclaration; doc detection takes an explicit docNode for that case.
- Doc coverage counts exported top-level symbols only (methods excluded) —
  matches the design's "under-documented public surface" intent.
